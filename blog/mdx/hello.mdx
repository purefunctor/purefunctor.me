export const title = "Hello, ReasonML"
export const description = "The joys of using simpler language."

# Hello, ReasonML

```python
def factorial(n):
  if n == 1:
    return 1
  else:
    n * factorial(n - 1)
```

```ocaml
let worthIt = "Absolutely!"
```

```haskell
let worthIt = "Absolutely!"
```

```rust
fn worthIt() {
  println!("Absolutely!");
}
```
